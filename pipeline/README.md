# pipeline/ — Engine

The shared deterministic tooling (the Engine layer): counting, exhaustive
search, verification, register generation, versioning. Tools detect and
verify; they never implement.

**Status:** nothing built. Phase 1 tooling begins only after Phase 0 is
settled on paper.

# The Entrepreneur's Blueprint

Collection — 5 distinct volumes + 1 combined collection volume. Each volume
has its own folder (`volume-1` … `volume-5`, `combined-collection`).

**Voice palette:** the largest of the works (many voices). Collection-level
palette applies across volumes unless a volume overrides it.

**Status:** scaffold only. Volume titles pending — folders named
`volume-N`; rename once titled.
